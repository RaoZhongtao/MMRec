embedding_size: 128
n_ui_layers: [4]
n_layers: 1
#Sports Parameters:['seed', 'n_ui_layers', 'image_knn_k', 'text_knn_k', 'reg_weight', 'dropout_rate']
# =(999, 4, 20, 20, 1e-05, 0)

# Beauty(999, 4, 10, 20, 1e-05, 0)
# (999, 4, 20, 20, 1e-05, 0)
learning_rate_scheduler: [0.96, 50]
lambda_coeff: 0.9
reg_weight: [1e-5]
learning_rate: 0.001

cl_loss: 0.01 
temperature: 0.2
image_knn_k: [10, 20]
text_knn_k: [20]

dropout_rate : [0]

hyper_parameters: ["n_ui_layers", "image_knn_k", "text_knn_k", "reg_weight", "dropout_rate"]