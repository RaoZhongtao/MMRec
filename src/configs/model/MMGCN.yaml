embedding_size: 128
n_layers: 2
reg_weight: [0]
learning_rate: [0.0001]

hyper_parameters: ["reg_weight", "learning_rate"]

# Parameters:['seed', 'reg_weight', 'learning_rate']=(999, 0, 0.0001)
# (999, 0, 0.0001)

